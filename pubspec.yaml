name: tutorial_app_wk1
description: A new Flutter project.

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  contacts_service: ^0.4.6
  flutter_dotenv: ^2.1.0
  flutter_redux: ^0.6.0
  redux: ^4.0.0
  redux_thunk: ^0.3.0
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env.development
    - .env.production
